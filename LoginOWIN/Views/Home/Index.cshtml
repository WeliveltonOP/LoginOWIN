
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> 
        Olá @User.Identity.Name <br />
        Cargo @(((System.Security.Claims.ClaimsIdentity)User.Identity).FindFirst(System.Security.Claims.ClaimTypes.Role).Value) <br />
       <a href="@Url.Action("LogOut", "Account")">Sair</a>
    </div>
</body>
</html>
